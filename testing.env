# Copy this file to .env and edit the values
# Create a secret key using the django framework
# or use the provided secret key
# code :from django.core.management.utils import get_random_secret_key
#       get_random_secret_key()
SECRET_KEY = c93gh$t31f((a59=2@^u2^19hi#vg+81&8p7yn=#1x6bhn@2f_
# Set DEBUG to True for development, False for actual use
DEBUG = False
# ALLOWED_HOSTS is a comma-separated list of hosts that can access the app.
# You can use wildcard chars (*) and IP addresses. Use * for any host.
ALLOWED_HOSTS = localhost, 127.0.0.1, ::1, testserver
TIME_ZONE = UTC
CALLBACK = http://bogus/callback